<%= form_for(@permissaoacesso) do |f| %>
  <% if @permissaoacesso.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@permissaoacesso.errors.count, "error") %> prohibited this permissaoacesso from being saved:</h2>

      <ul>
      <% @permissaoacesso.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descricao %><br>
    <%= f.text_field :descricao %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
